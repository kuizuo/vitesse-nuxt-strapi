{
  "name": "vitesse-nuxt3-strapi",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "wait-on http://localhost:1337/admin && npm --prefix frontend run dev",
    "dev": "cross-env NODE_ENV=development FORCE_COLOR=1 npm-run-all -l -p dev:*"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5",
    "wait-on": "^6.0.1"
  }
}
